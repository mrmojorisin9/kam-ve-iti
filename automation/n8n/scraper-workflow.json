{
  "name": "Kam denes — scraper (emedjimurje)",
  "meta": {
    "note": "Faza 6-7, ADR-020, Korak 5. Uvesti u n8n instancu (Import from File) nakon docker compose up — vidi automation/deploy/README.md. 'Cron' cvor okida dnevno, 'Execute Command' poziva venv python izravno u istom kontejneru (Dockerfile instalira automation/ + ovisnosti u /repo)."
  },
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [{ "field": "days", "daysInterval": 1 }]
        }
      },
      "name": "Cron (dnevno)",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1,
      "position": [240, 300]
    },
    {
      "parameters": {
        "command": "/repo/automation/.venv/bin/python -m automation.pipeline --source emedjimurje"
      },
      "name": "Pokreni scraper (emedjimurje)",
      "type": "n8n-nodes-base.executeCommand",
      "typeVersion": 1,
      "position": [520, 300]
    }
  ],
  "connections": {
    "Cron (dnevno)": {
      "main": [[{ "node": "Pokreni scraper (emedjimurje)", "type": "main", "index": 0 }]]
    }
  }
}
