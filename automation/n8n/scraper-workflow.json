{
  "name": "Kam denes — scraper (emedjimurje)",
  "meta": {
    "note": "Faza 6-7, ADR-020, Korak 5. Uvesti u n8n instancu (Import from File) nakon docker compose up — vidi automation/deploy/README.md. 'Cron' cvor okida dnevno, 'HTTP Request' cvor poziva automation servis (zaseban kontejner, http://automation:8000/run) — ne Execute Command, jer n8n-ov sluzbeni image nema package manager za Python (Docker Hardened Image)."
  },
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [{ "field": "days", "daysInterval": 1 }]
        }
      },
      "name": "Cron (dnevno)",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1,
      "position": [240, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://automation:8000/run",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [{ "name": "source", "value": "emedjimurje" }]
        },
        "options": {}
      },
      "name": "Pokreni scraper (emedjimurje)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [520, 300]
    }
  ],
  "connections": {
    "Cron (dnevno)": {
      "main": [[{ "node": "Pokreni scraper (emedjimurje)", "type": "main", "index": 0 }]]
    }
  }
}
