# components

Ponovno iskoristivi UI dijelovi (React komponente). Svaka komponenta u vlastitom
podfolderu kad naraste (npr. `components/EventCard/EventCard.tsx`), dok je mala
može ostati kao pojedinačna `.tsx` datoteka.
